import { ResaltarPokemonDirective } from './resaltar-pokemon';

describe('ResaltarPokemonDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarPokemonDirective();
    expect(directive).toBeTruthy();
  });
});